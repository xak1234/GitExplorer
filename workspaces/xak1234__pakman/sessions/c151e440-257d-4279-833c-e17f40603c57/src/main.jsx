import React from 'react'
import { createRoot } from 'react-dom/client'

// Placeholder entry to confirm Vite is wired up. The app still runs from inline script for now.
console.log('[Vite] Loaded /src/main.jsx')

// If you want to progressively migrate, expose a window hook here and call it from inline code.
// Example (later):
// if (window.mountPakmanApp) {
//   const root = createRoot(document.getElementById('root'))
//   root.render(React.createElement(window.mountPakmanApp))
// }
